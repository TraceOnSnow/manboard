import { useState } from "react"; import type { Box, BoxDelete } from "../types/board";
export function DeleteBoxDialog({box,candidateBoxes,onConfirm,onClose}:{box:Box;candidateBoxes:Box[];onConfirm:(value:BoxDelete)=>Promise<void>;onClose:()=>void}){const [mode,setMode]=useState<"delete"|"move">("delete"),[target,setTarget]=useState("");const submit=async(e:React.FormEvent)=>{e.preventDefault();if(mode==="move"&&!target)return;await onConfirm(mode==="delete"?{taskDisposition:"delete"}:{taskDisposition:"move",targetBoxId:target});onClose()};return <div className="fixed inset-0 z-50 grid place-items-center bg-zinc-950/30 p-4"><form onSubmit={submit} className="w-full max-w-md rounded-2xl bg-white p-5 shadow-xl"><h2 className="text-lg font-semibold">删除「{box.title}」</h2><p className="mt-2 text-sm text-zinc-500">选择如何处理这个 Box 中的任务。</p><label className="mt-4 flex gap-2 text-sm"><input type="radio" checked={mode==="delete"} onChange={()=>setMode("delete")}/>删除 Box 及其中任务</label><label className="mt-3 flex gap-2 text-sm"><input type="radio" checked={mode==="move"} onChange={()=>setMode("move")}/>迁移任务到其他 Box</label>{mode==="move"&&<select required value={target} onChange={e=>setTarget(e.target.value)} className="mt-3 w-full rounded-lg border px-3 py-2"><option value="">选择目标 Box</option>{candidateBoxes.filter(item=>item.id!==box.id).map(item=><option key={item.id} value={item.id}>{item.title}</option>)}</select>}<div className="mt-5 flex justify-end gap-2"><button type="button" onClick={onClose} className="rounded-lg px-3 py-2 text-sm">取消</button><button disabled={mode==="move"&&!target} className="rounded-lg bg-red-600 px-3 py-2 text-sm text-white disabled:opacity-50">删除</button></div></form></div>}
